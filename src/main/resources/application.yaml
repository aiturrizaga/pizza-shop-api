server:
  port: ${PORT:8080}
  servlet:
    context-path: /${spring.application.name}

spring:
  application:
    name: vgshop
  datasource:
    url: jdbc:postgresql://localhost:5432/pizza_shop
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
